overwrite: true
schema: ${GRAPHQL_URL}
documents: "**/*.{gql,graphql}"
generates:
  ./graphql/generated/schema.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
